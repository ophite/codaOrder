(function () {
    'use strict';
    var app = angular.module('app', [
        // Angular modules 
        //'ngAnimate',
        //'ngRoute',
        'ngResource',

        // Custom modules 

        // 3rd Party Modules
        //'ui.grid',
        'ngGrid'
    ])

    //app.config(function ($locationProvider, $httpProvider) {
    //});

    //app.run(['$rootScope',
    //    function ($rootScope) {
    //    }
    //]);
})();